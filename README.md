# RegMonkey
RegMonkey is a prototype framework for lightweight verification of empirical regression models under data disclosure constraints.

The project explores regression verification using compact algebraic summaries rather than full computational replication.

Current scope:
- OLS models
- analysis-ready datasets
- signed verification certificates
- lightweight verifier

Status:
Early-stage research prototype.
